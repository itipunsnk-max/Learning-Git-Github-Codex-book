document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('pre').forEach(pre=>{const b=document.createElement('button');b.className='copy';b.type='button';b.textContent='คัดลอก';b.addEventListener('click',async()=>{await navigator.clipboard.writeText(pre.querySelector('code')?.innerText.trim()||'');b.textContent='คัดลอกแล้ว';setTimeout(()=>b.textContent='คัดลอก',1400)});pre.appendChild(b)});const top=document.querySelector('.top');window.addEventListener('scroll',()=>top?.classList.toggle('show',scrollY>500));top?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));const input=document.querySelector('[data-search]');input?.addEventListener('input',e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('[data-search-item]').forEach(el=>el.hidden=q&&!el.textContent.toLowerCase().includes(q))});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>document.querySelectorAll('.toc a').forEach(x=>x.classList.toggle('active',x===a))));});
